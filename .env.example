# Supabase (client-side — anon key is safe to expose with RLS enabled)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Anthropic (server-side only — NOT prefixed with VITE_ to keep it out of the browser)
ANTHROPIC_API_KEY=your-anthropic-api-key-here
